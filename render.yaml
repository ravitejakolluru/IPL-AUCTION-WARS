services:
  - type: web
    name: ipl-auction-wars
    runtime: node
    plan: free
    buildCommand: npm install && npm run install-all && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
